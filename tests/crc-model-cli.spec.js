

describe('`eslint-plugin-crc`\'s command-line interface (cli)', () => {

    it('requires an argument to specify which files to analyze', () => {

    });

    it('accepts an optional -o or --output argument the report');

});
